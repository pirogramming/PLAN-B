--- 페이지 1 ---
101 0 ZI HO (0 [= 10 rf 00 Data Structure injoong Rim, Dongguk University

--- 페이지 2 ---
ㆍ Table of Contents » Introduction » Recursion » C++ Programming =» Tree 1 =» Array » Tree2 » Stack =» Graph 1 =» Queue ㅁ Graph 2 =» List 1 » Sort, Search » List 2 Circular Linked List Doubly Linked List Linked List with Header Node Sorted Linked List Circular Doubly Linked List Minjoong Rim, Dongguk University 2 Data Structure

--- 페이지 3 ---
Sorted vs. Unsorted Lists » Sorted elements with key » Unsorted elements Kinds of Linked Lists =» Linked List head pointer =» Circular Linked List head pointer tail pointer =» Doubly Linked List head and tail pointers “FHF null tail forward and backward traversals ends with NULL We will see - Circular Linked List - Doubly Linked List - Sorted Linked List - Circular Doubly Linked List Minjoong Rim, Dongguk University Data Structure

--- 페이지 4 ---
Circular Linked List Minjoong Rim, Dongguk University 4 Data Structure

--- 페이지 5 ---
ㆍ Circular Linked List =» A linked list where all nodes are connected to form a circle =» There is no NULL atthe end ㆍ Advantages » Any node can be a starting point We can traverse the whole list by starting from any pont We just need to stop when the first visited node is visited again » Useful in applications to repeatedly go around the list When multiple applications are running on a PC, it is convenient for the operating system to use a circular list so that when it reaches the end of the list it can cycle around to the front of the list =» Useful for implementation of queue head = tail->next We don't need to maintain two pointers for front and rear Fail We can maintain a pointer to the last inserted node and front can always be obtained as next of last front rear Minjoong Rim, Dongguk University 5 Data Structure

--- 페이지 6 ---
Insert head» null Insert to empty list eel head Insert to rear 더피 Print assuming that the list is not empty head head = NULL; head = new Node(10); head->next = head; Node* p = head; for (; p->next != head; p = p->next); p->next = new Node(20, head); Node* p = head; for (int i = 0; i < n; i++, p = p->next) { } Node* p = head; do { cout << p->data << ""; p = p->next; } while (p != head); cout << p->data << ""; Minjoong Rim, Dongguk University 6 Data Structure

--- 페이지 7 ---
ㆍ Queue using Circular Linked List tail tail = NULL: null tail tail = new Node(10); Insert(10) tail->next = tail; // make circular when # of nodes = 0 tail tail->next = new Node(20, tail->next); Insert(20) tail = tail->next; when # of nodes > O tail 이 Data 0 = tail->next->data; 미 DELETE(tail->next); return d; tail Data 0 = tail->next->data: Delete() — 20 t delete tail; tail = NULL: null return d: Delete() — 10 when # of nodes > 1 when # of nodes = 1 Minjoong Rim, Dongguk University 7 Data Structure

--- 페이지 8 ---
Application of Circular Linked List. void Insert(Data d) { // insert at the last if (head == NULL) { // insert at head head = new Node(d); head->next = head; } else { // if not empty, insert at the last Node* p = head; // traverse to end for (; p->next != head; p = p->next); p->next = new Node(d, head); } } #include <iostream> main.cpp using namespace std; typedef string Data; class Node { // node for linked list Data data; Node* next; // next node Node(Data 0=" ", Node* n=NULL) { data = d; next = n; } // constructor friend class CircularLinked_List; }; cme PLETE { CircularLinkedList c; c.Insert("Dynamite"); c.Insert("Idol"); c.Insert("Butter"); c.PrintList(10); // play 10 musics return 0; }; class CircularLinkedList { Node* head; public: CircularLinkedList() { head = NULL; } void PrintList(int n) {// print n lists Node* p = head; } for (int i = 0; i <n; i++, p = p->next) { . cout << p->data <<", ";}} | Dynamite, Idol, Butter, Dynamite, Idol, Butter, | Dynamite, Idol, Butter, Dynamite | Minjoong Rim, Dongguk University 8 Data Structure

--- 페이지 9 ---
Hot potato is a party game that involves players passing an object around whle music plays. The player who is holding the object when the music stops is eliminated. #include <iostream> main.cpp #include <cstdlib> using namespace 510; typedef string Data; class Node { // node for linked list Data data; Node* next; // next node Node(Data d="", Node* n=NULL) { data = d; next = n; }// constructor friend class HotPotato; }; class HotPotato { Node* head; public: HotPotato() { head = NULL; } void Play(int time) { // play for time seconds Node* p = head; // start from the first one while (time > 0) { // while music plays time -= rand() % 3 + 1; // decrease 1~3 cout << p->data <<""; // print player Minjoong Rim, Dongguk University 9 p = p->next; } // pass to next cout << endl << p->data << "is out\n"; } void Insert(Data d) { // insert at the last if (head == NULL) { // insert at head head = new Node(d); head->next = head; } else { // if not empty, insert at the last Node* p = head; // traverse to end for (; p->next != head; p = p->next); p->next = new Node(d, head); } } }; int main() HotPotato h; h.Insert("Kim"); h.Insert("Lee"); h.Insert("Park"); h.Insert("Choi"); h.Play(15); // play for 15 seconds | Kim Lee Park Choi Kim Lee | Park is out Data Structure

--- 페이지 10 ---
Using circular list, queue can be implemented without traversing. #include <iostream> else { head = new Node(d, head); #include <cstdlib> tail = head; } } // move to tail ™ain.cpp using namespace std; Data Delete() { // delete head (tail->next) typedef int Data; front rear if (tail == NULL) Error("Queue empty"); #define DELETE(a) { Node* temp = a; \ Data d = head->data; // head data a = a->next; delete temp; } if (head == tail) { // if only one node #define head tail->next // head = tail->next delete tail; tail = NULL; } // empty list void Error(string s) { cerr << s; exit(-1); } else DELETE(head); // delete head Class Node { // node for linked list return d; } Data data; Node* next; }; Node(Data d=0, Node* n=NULL) { data = 0; next = n; }// constructor Insert O(1) O(1) O(n) O(1) ! friend class QueueCircularLinkedList; Delete O(1) O(n) O(1) O(1) class QueueCircularLinkedList { Peek O(1) O(1) O(1) 9601) Node tail; // head = tail->next int main() { public: QueueCircularLinkedList q; QueueCircularLinkedList() { tail = NULL; } q.Insert(10); g.lInsert(20); q.lnsert(30); void Insert(Data d) { // insert at the tail cout << q.Delete() << " "; cout << q.Delete(); if (tail == NULL) { // if list is empty return 0; tail = new Node(d); head = tail; } } Minjoong Rim, Dongguk University 10 Data Structure

--- 페이지 11 ---
Doubly Linked List Minjoong Rim, Dongguk University 11 Data Structure

--- 페이지 12 ---
ㆍ Doubly Linked List =» Anode contains an extra pointer in addition to next pointer, typically called previous pointer Node(data, next, prev) class Node { xt link Data data; ne in = . previous 아이 Node* next; Node* prev; =» Two pointers in the list First node pointer (head) first header Last node pointer (tail) last _ trailer class DoublyLinkedList { Node* head; 아어 아려 20 Ff 30 잼 Nogersath null tail 때 » 1116 two node links (head and tail) allow traversal of the list in either direction Print forward, Print backward =» The prev part of the first node and the next part of the last node will always contain null indicating end in each direction Minjoong Rim, Dongguk University 12 Data Structure

--- 페이지 13 ---
ㆍ Advantage of Doubly Linked List =» List can be traversed in both forward and backward direction =» The delete operation is more efficient There is no need to keep track of the previous node during traversal =» Wecan quickly insert a new node before a given node ㆍ Disadvantage of Doubly Linked List =» Every node requires extra space for a previous pointer =» All operations require an extra pointer (previous) to be maintained ㆍ Doubly Linked List as Circular =» In doubly linked list, the next pointer of the last node points to the first node and the previous pointer of the first node points to the last node making the circular in both directions Circular Doubly Linked List Minjoong Rim, Dongguk University 13 Data Structure

--- 페이지 14 ---
ㆍ Main Operations (06046 ADT) 1600 null =» Insert Front null Fail head null » Insert Rear Insert Front 20 pas) ae =» Delete Front Insert Front 10 head ZI 10 Rf 20 fe null = Delete Rear head null » Peek Front Insert Rear 30 null =[10 [20 Ff 30 = tail =» Peek Rear head ㅣ Delete Front -> 10 I =| 이거 30 [= wail » Is List Empty? . _. head null ㆍ Other Operations Delete Rear > 30 i720" | class DoublyLinkedList { =» Print List 때 void InsertFront(Data 0); void InsertRear(Data d); Data DeleteFront(); We can easily access both front and rear (i.e. O(1)) thanks to head and tail pointers Deque, Stack, Queue can be implemented with O(1) Data DeleteRear(); Data PeekFront(); Data PeekRear(); Data Structure Array Linked Minjoong Rim, Dongguk University 14 Data Structure

--- 페이지 15 ---
Insert Insert Front 30 head.» null . . null tail if there is no node head—» null null data next prev ail tail a) 307 new Node(30, NULL, NULL) Node* n = new Node(); n->data = d; “그 null if (IsEmpty()) // if there is no node a vere null tail head = tail = new Node(d, NULL, NULL); tail =n: . head =n; Insert Front 20 head null null = 30 fe if there exists a node h ll data next prev Se new Node(20, head, NULL) nead +120 L*[ 30 null | if (IlsEmpty() == false) // if there exists a node null Node* n = new Node(); n->data = 0; n->next = head; . n->prev = NULL; tail head = head->prev = new Node(d, head, NULL); } j.054-~prey = n: head =n; Minjoong Rim, Dongguk University 15 Data Structure

--- 페이지 16 ---
ㆍ Delete Delete Front head if (nead->next != NULL) Delete Front { // multiple nodes if (nhead->next == NULL) head null | ¢ // single node head = head->next; null pa ET) ae { 9 . head—» delete head; | +, delete head->prev; head—» null head = tail = NULL; head->prev = NULL; null “= tail } 1, |} ㆍ Traverse Linked List Void PrintList() { for (Node* p = head; p != NULL; p = p->next) { need 1 아 거 22 F’[ 30 null cout << p->data << ""; // print forward null tail } for (Node* p = tail; p != NULL; p = p->prev) { cout << p->data << ""; // print backward eee | [head] => 10 => 20 => 30 => [null] | [tail] => 30 => 20 => 10 => [null] | ee ee a a a ee en wn ee ee ne ee I Minjoong Rim, Dongguk University 16 Data Structure

--- 페이지 17 ---
In doubly linked list, navigation is possible in both ways either forward and backward. // Not yet completed DoublyLinkedList.h #include "DoublyLinkedList.h" main.cpp typedef int Data; int main() head—» null null = tail d.InsertFront(20); need {20 | mi d.InsertFront(10); need 10 Lf 20 군 mi class DoublyLinkedList { DoublyLinkedList d; void InsertFront(Data d); void InsertRear(Data 0); Data DeleteFront(); Data DeleteRear(); void PrintList(); d.InsertRear(30); need 1 10 Feet 20 Ff 30 nu d.PrintList(); cout << d.DeleteFront() << endl; | [head] => 10 => 20 => 30 => [null] | d.PrintList(); head [soe null | [tail] => 30 => 20 => 10 => [null] | null tail ! in 4) => 20 => 30 => [null ‘| cout << d.DeleteRear() << endl; | 11680] => <0 => 00 == [nu || d.PrintList(); head null | [tail] => 30 => 20 => [null] ‘| return 0; null pas FE) Pac | 30 | [head] => 20 => [null] [tail] => 20 => [null] Minjoong Rim, Dongguk University 17 Data Structure

--- 페이지 18 ---
Class for Doubly Linked List. #include <iostream> DoublyLinkedList.h || public: #include <cstdlib> using namespace 510; typedef int Data; struct Node { // node for doubly linked list Data data; Node* next; Node* prev; // next, previous Node(Data d=0, Node* n=NULL, Node* p=NULL) { data = d; next = n; prev =p; } }; class DoublyLinkedList { protected: Node* head; Node® tail; // first, last DoublyLinked_List() { head = tail = NULL; } void InsertFront(Data d); void InsertRear(Data d); Data DeleteFront(); Data DeleteRear(); Data PeekFront(); Data PeekRear‘(); void PrintList(); bool IsEmpty() { return head == NULL; } Minjoong Rim, Dongguk University Data Structure

--- 페이지 19 ---
Implementation of doubly linked list. #include "DoublyLinkedList.h" void Error(string s) { cerr << s; exit(-1); } DoublyLinkedList.cpp void DoublyLinkedList::InsertFront(Data 0) { // Insert node to the front of doubly linked list if (IsEmpty()) head = tail = new Node(d, NULL, NULL); else head = head->prev = new Node(d, head, NULL); } void DoublyLinkedList::InsertRear(Data 0) { // Insert node to the rear of doubly linked list if (IsEmpty()) head = tail = new Node(d, NULL, NULL); else tail = tail->next = new Node(d, NULL, tail): void DoublyLinkedList::PrintList() { // Print doubly linked list cout << "[head] => "; for (Node* p = head; p != NULL; p = p->next) cout << p->data << "=>"; // forward cout << "[null]" << endl << "[tail] => "; for (Node* p = tail; p != NULL; p = p->prev) cout << p->data << "=>"; // backward cout << "[null]" << endl; Minjoong Rim, Dongguk University 19 Data Structure

--- 페이지 20 ---
Implementation of doubly linked list (continued). // continued DoublyLinkedList.cpp || Data DoublyLinkedList::DeleteRear() { // Delete node from the rear if (IsEmpty()) Error("List is empty"); Data d = tail->data; if (tail->prev == NULL) { delete tail; head = tail = NULL; } else { tail = tail->prev; delete tail->next; tail->next = NULL; } return d; Data DoublyLinkedList::DeleteFront() { // Delete node from the front if (IsEmpty()) Error("List is empty"); Data d = head->data; head if (nead->next == NULL) { __ null delete head; head = tail = SBE ) else { head = head->next; head =| 20 | 30 [= delete head->prev; head head->prev = NULL; null } head return d; } head } Data DoublyLinkedList::PeekRear() { // Get node in the rear if (IsEmpty()) Error("List is empty"); return tail->data; Data DoublyLinkedL ist: PeekFrontt) { // Get node in the front if (IsEmpty()) Error("List is empty"); return head->data; } } Minjoong Rim, Dongguk University 20 Data Structure

--- 페이지 21 ---
06046 using Doubly Linked List. #include "DoublyLinkedList.h" main.cpp |: 10 20 30 40 50 60 | [head] => 30 => 20 => 10 => 40 => 50 => 60 => 000]: int main() ' [tail] => 60 => 50 => 40 => 10 => 20 => 30 => [null] | | 30 20 10 40 50 60 DoublyLinkedList d; "Array List: OR int n = 0; Insert Front, Delete Front requires shift operations for (int i = 0; i < 3; i++) { (Singly) Linked List: d.InsertFront(++n * 10); // 10 20 30 Insert Rear, Delete Rear, Peek Rear requires traversing to the end cout << d.PeekFront() << ""; }// 30 20 10 for (int i = 0; i < 3; i++) { d.InsertRear(++n * 10); // 40 50 60 cout << d.PeekRear() << ""; }// 30 20 10 40 50 60 Insert Front O(1) cout << 6001; Insert Rear O(1) d.PrintList(); while (d.lsEmpty() == false) { Delete Front = O(1) cout << d.DeleteFront() <<" "; Delete Rear O(1) } return 0: Peek Front O(1) Peek Rear O(1) Minjoong Rim, Dongguk University 21 Data Structure

--- 페이지 22 ---
Image viewer using Doubly Linked List. #include "DoublyLinkedList.h" class IlmageViewer : public DoublyLinkedList { Node®* current; // current node to view public: void View() { // show the current node if (current == NULL) cout << "Nothing to view "; else cout << current->data << "";} void ViewFront() { current = head; View(); } void ViewRear‘() { current = tail; View(); } void ViewNext() { if (current == NULL) return; current = current->next; View(); } void ViewPrev() { if (current == NULL) return; current = current->prev; View(); } }; Minjoong Rim, Dongguk University main.cpp 22 int main() ImageViewer v; v.InsertRear(10); v.InsertRear(20); v.InsertRear(30); v.InsertRear(40); v.InsertRear(50); v.InsertRear(60); v.ViewFront(); // show the first image cout << endl: v.ViewRear(); // show the last image cout << endl: return 0; class DoublyLinkedList { void InsertFront(Data d); a void InsertRear(Data d); 40 20 30 40 Data DeleteFront(): ' 60 50 40 30 Data DeleteRear(); | 40 50 60 }; Data Structure null v.ViewNext(); v. ViewNext(); v. ViewNext(); v.ViewPrev(); v.ViewPrev(); v.ViewPrev(); v.ViewNext(); v.ViewNext(); v. ViewNext();

--- 페이지 23 ---
Alternative Implementation of Linked List: Linked List with Header Node Minjoong Rim, Dongguk University 23 Data Structure

--- 페이지 24 ---
ㆍ Header Linked List s A variant of linked list » Header node (or dummy node) a special node at the beginning of the list Can be used to store some information such as the maximum, the minimum, or the number of nodes in the linked list ㆍ Advantage =» Easy to get information stored in header lf the header node stores the maximum, the minimum, or the number of nodes, we can easily get the information without the need to traverse the list » We don't need to check whether head is null because header node will always exist Insertion and deletion of nodes become easier Minjoong Rim, Dongguk University 24 Data Structure

--- 페이지 25 ---
ㆍ Header node implementation header node or dummy node » Additional header node at the front head» 4+[ 10 }+[ 20 [30 > null Header node is inserted in constructor class LinkedList { // Scheme 1 Node* head; LinekdList() { // constructor head = new Node(0, NULL); =» Header node instead of header pointer // Scheme 3 // Scheme 4 class LinkedList : public Node { class LinkedList { // inherited from Node Data data; LinkedList* next; We will use Scheme 2 // Scheme 2 class LinkedList { Node head; // include Node class }; / class Node not used Minjoong Rim, Dongguk University 25 Data Structure

--- 페이지 26 ---
Insert to head Node* p = &head; p->next = new Node(20, p->next); 더 Insert to other position p->next = new Node(20, p->next); ㆍ Delete Delete from head Node* p = &head; Node* temp = p->next; p->next = p->next->next; delete temp; Delete from other position Node* temp = p->next; p->next = p->next->next; delete temp; Minjoong Rim, Dongguk University 26 Data Structure

--- 페이지 27 ---
Traverse Linked List for (Node* p = head.next; p != NULL; p = p->next) cout << p->data << ""; I [파기 10 F+[ 20 F+| 30 [+ 내 for (Node* p = &head; p->next != NULL; p = p->next) p->next = ... Kinds of Linked Lists Header Pointer Header Node (Singly) Linked List head—s{ 10 }+[ 20 }4[ 30 b> null head 10 }+{ 20 [4] 30 b> null Circular Linked List i Ee Ed ee . . head null Doubly Linked List __,,, ZI 10 [2] 20 AI 30 = tail We will see: - Sorted Linked List (with Header Node) - Circular Doubly Linked List (with Header Node) 27 Data Structure Circular Doubly Linked List Minjoong Rim, Dongguk University

--- 페이지 28 ---
Sorted Linked List (with Header Node) Minjoong Rim, Dongguk University 28 Data Structure

--- 페이지 29 ---
ㆍ Sorted Linked List class Node { =» Nodes are sorted in ascending order using key Data data; Node* next; ㆍ Linked List with Header Node =» Header is a Node instead of a pointer class SortedLinkedList { Node head; SortedLinkedList() { head = Node(0, NULL); } }; void Insert(Data 0) { Node* p = &head; for (; p->next != NULL; p = p->next) { void PrintList() { if (Key(p->next->data) > Key(d)) break; cout << "[head] => "; for (Node* p = head.next; p != NULL; p = p->next) p->next = new Node(d, p->next); cout << p->data <<" =>"; head.datat++; cout << "[null]" << endl; } } Minjoong Rim, Dongguk University 29 Data Structure

--- 페이지 30 ---
ㆍ Main Operations (Sorted List ADT) fhead—> null = Insert Insert 30 null Insert element in sorted list =» Delete Insert 10 null Delete element with key Insert 20 =» Search null Find and return element with key . Ss h 20 — 20 » Is List Empty? ware null null ㆍ Other Operations Delete 20 — 20 = Print List =» Delete Front class SortedLinkedList { For simplicity, we assume that key is an integer type, while ‘Data’ can be any type (usually class including many fields). For example, if you search for student id, the student information can be returned. void Insert(Data d); Data Delete(int key); Data Search(int key); Data bool IsEmpty(); Structure Array Linked Minjoong Rim, Dongguk University 30 Data Structure

--- 페이지 31 ---
In this implementation, head is a class instead of a pointer. // Not yet completed SortedLinkedList.h|| #include "SortedLinkedList.h" main.cpp typedef int Data; int main() { class SortedLinkedList { SortedLinkedList a; fiead—> null a.Insert(30): [따기 30 }> null a.PrintList(); a.Insert(10); a.PrintList(); a.Insert(20); ull a.PrintList(); Coy cout << "Length = " << a.Length() << endl; cout << a.Delete(20) << endl; void Insert(Data 0); // insert a node Data Delete(int key); // delete node void PrintList(); // print list bool IsEmpty(); // 16 List empty? int Length(); // length of list | [head] => 10 => 30 => [null] ‘| a.PrintList(); LH nul | [head] => 10 => 20 => 30 => [null] ‘| cout << "Length ="; | Length = 3 ‘| cout << a.Length(); | 20 '| return 0; | [head] => 10 => 30 => [null] ! Minjoong Rim, Dongguk University 31 Data Structure

--- 페이지 32 ---
Class. #include <iostream> SortedLinkedList.h| | class SortedLinkedList { #include <cstdlib> protected: using namespace std; Node head; // header node const int SEARCH_FAILED = -1000000; virtual int Key(Data d) { return d; } typedef int Data; public: SortedLinkedList() { head = Node(0, NULL); } void Insert(Data d); // insert a node Data Delete(int key); // find & delete class Node { // node for linked list Data data; Node* next; // next node Node(Data d=0, Node* n=NULL) { data = 0; next = n; } friend class SortedLinkedList; }; Data DeleteFront(); // delete front Data Search(int key); // find node void PrintList(); // print list bool IsEmpty() { return head.next == NULL; } void PrintData(Data d); int Length() { return head.data; } Minjoong Rim, Dongguk University 32 Data Structure

--- 페이지 33 ---
Implementation. #include "SortedLinkedList.h" SortedLinkedList.cpp #define DELETE(a) { Node* temp = a; \ a = a->next; delete temp; } Data SortedLinkedList::Search(int key) { // Find node with key Node* p = head.next; for (; p != NULL; p = p->next) { if (Key(p->data) > key) break; if (Key(p->data) == key) return p->data; } cout << key <<" 16 not found" << endl; return SEARCH_FAILED; } Minjoong Rim, Dongguk University 33 void SortedLinked_List::PrintList() { // Print linked list cout << "[head] =>"; Node* p = head.next; for (; p != NULL; p = p->next) cout << p->data <<" =>"; cout << "[null]" << endl; } void SortedLinked_List::PrintData(Data 0) { // Print search result if (Key(d) != SEARCH_FAILED) cout << Key(d) << " is found" << endl; } Data SortedLinkedList::DeleteFront() { // Delete front node Data d = head.next->data; DELETE(head.next); head.data--; return d; } Data Structure

--- 페이지 34 ---
Implementation (continued). // continued SortedLinkedList.cpp || void SortedLinked_List::Insert(Data d) { // Insert a node in the sorted list Node* p = &head; for (; p->next != NULL; p = p->next) { if (Key(p->next->data) > Key(d)) break; Data SortedLinkedList::Delete(int key) { // Find and delete node Node* p = &head; for (; p->next != NULL; p = p->next) { if (Key(p->next->data) > key) break; if (Key(p->next->data) == key) { p->next = new Node(d, p->next); head.data++; Data 0 = p->next->data; } DELETE(p->next); head.data--; return d; } } | Algorithm gray ist Linke List cout << key <<" is not found" << endl; return SEARCH_FAILED; Search O(log n) O(n) } Select O(1) O(n) Insert O(n) O(n) Delete O(n) O(n) Minjoong Rim, Dongguk University 34 Data Structure

--- 페이지 35 ---
This program prints polynomial using sorted linked list. #include <iostream> main.cpp Node* p = &head; // find position & insert using namespace std; for (; p->next != NULL; p = p->next) class Node { // node for linked list if (p->next->expon ~ e) break; int coeff, expon; // coefficient, exponent p->next = new Node(c, e, p->next); } Node* next; void Print() { // print polynomial void Print() { // print coefficient and exponent Node* p = head.next; cout << coeff; for (; p != NULL; p = p->next) p->Print(); if (expon != 0) cout << " x4" << expon; cout << endl; } if (next != NULL) cout << "+"; } }; Node (int c=0, int e=0, Node* n=NULL) { coeff = c; expon = e, next =n; } int main() friend class Polynomial; { // array list: int af] = {3, 0, 0, 2, 0, 0, 0, 7}; }; Polynomial p; p.Insert(3, 0); p.Insert(5, 7); p.Insert(2, 3); class Polynomial { // sorted descending order cout << "p ="; p.Print(); Node head; // header node return 0; public: Polynomial() { head.next = NULL; } void Insert(int c, int e) { if (Cc == 0) return; // if zero, do not insert Minjoong Rim, Dongguk University 35 Data Structure

--- 페이지 36 ---
Sorted Linked List for Student Info. #include "Student.h" // Student from Stack class Node { Student data; Node* next; Node(Student d, Node* n=NULL) { data = 0; next = n; } friend class StudentSortedLinked_List; }; class StudentSortedLinked_List { Node head; public: StudentSortedLinkedList() { head.next = NULL; } void Insert(Student d) { Node* p = &head; for (; p->next != NULL; p = p->next) if (p->next->data.Key() > d.Key()) break; p->next = new Node(d, p->next); } void PrintList() { struct Student { int id; string name; int age; void Print(); int Key(); Minjoong Rim, Dongguk University 36 cout << "[head]" << endl; main.cpp Node* p = head.next; for (; p != NULL; p = p->next) { cout <<" =>"; p->data.Print(); } cout << "=> [null]" << endl; } }; int main() { StudentSortedLinkedList a; a.Insert(Student(2345, "Lee", 21)); a.Insert(Student(1234, "Kim", 20)); a.Insert(Student(3456, "Park", 20)); a.PrintList(); return 0; => ID = 1234; Name = Kim; Age = 20 | => ID = 2345; Name = Lee; Age = 21 | => ID = 3456; Name = Park; Age = 20 | => [null] Data Structure

--- 페이지 37 ---
Bank-customer simulation with deadline (Earliest Deadline First). #include "SortedLinkedList.h" main.cpp bool Waiting() { return ! a.lsEmpty(); } #define SERVICE_TIME 3 // fixed service time || }; Priority Queue: Insertion is performed in the order of arrival and deletion is class BankDeadline { int main() performed based on priority SortedLinked_List a; { // Using sorted list based on deadline intnumServed; // # of served customers const float arrivalProb = 0.3; int numFailed; // # of deadline failures BankDeadline b; deadline public: int serviceTime = 0; BankDeadline() { for (int time = 0; time < 10000; time++) { numServed = numFailed = 0; } if (b.lsArrived(arrivalProb) { // if arrived void NewCustomer(int d) { a.Insert(d); } // random deadline int ServeCustomer(int time) { int deadline = time + 10 + rand() % 20; int deadline = a.DeleteFront(); b.NewCustomer(deadline); } bool failure = time > deadline; if (--serviceTime <= 0 && b.Waiting()) numServed++; service Time = b.ServeCustomer(time); if (failure) { numFailed++; return 0; } } else return SERVICE_TIME; } b.Report(); bool IsArrived(float arrivalProb) { return 0; return rand() < arrivalProb * RAND_MAxX; } || } void Report() { cout << "Failure ratio = " ： << double(numFailed) / numServed; } 3 | Failure ratio = 0.0298758 |} class SortedLinkedList { void Insert(Data d); Data DeleteFront(); bool IsEmpty(); Minjoong Rim, Dongguk University 37 Data Structure

--- 페이지 38 ---
Circular Doubly Linked List (with Header Node) Minjoong Rim, Dongguk University 38 Data Structure

--- 페이지 39 ---
¢ Doubly Linked list as Circular =» In doubly linked list, the next pointer of the last node points to the first node and the previous pointer of the first node points to the last node making the circular in both directions Circular Doubly Linked List Doubly Linked List ㆍ Linked List with Header Node =» Header is a Node instead of a pointer =» In doubly linked list with a header node, the next pointer of the last node points to the header node and the previous pointer of the first node points the the header node making the circular in both directions class Node { class CircularDoublyLinkedList { Circular Doubly Linked List Data data; Node head; ircu ar ou y InKe Is Node* next; 때 with Header Node Node* prev: Minjoong Rim, Dongguk University 39 Data Structure

--- 페이지 40 ---
ㆍ Main Operations (06046 ADT) oo =» Insert Front =» Insert Rear Insert Rear 10 feed [20 5 = Delete Rear » Is List Empty? . class CircularDoublyLinkedList ㆍ Other Operations ： { s Print List void InsertFront(Data d); void InsertRear(Data d); Data DeleteFront(); We can easily access both front and rear (i.e. O(1)) thanks to head (next of header) and tail (prev of header) pointers Data DeleteRear‘(); Data PeekFront(); Data PeekRear‘(); bool IsEmpty(); Data Structure Array Linked Linked Minjoong Rim, Dongguk University 40 Data Structure

--- 페이지 41 ---
Main operations of circular doubly linked list. typedef string Data; class CircularDoublyLinkedList { void InsertRear(Data 0); —// insert rear Data DeleteRear‘(); // delete rear void PrintList(); // print list }; [head] => 10 => 20 => 30 => [tail] | [tail] => 30 => 20 => 10 => [head] | 30 i [head] => 10 => 20 => [tail] | [tail] => 20 => 10 => [head] r 1 1 1 ㅣ // Not yet completed CircularDoublyLinkedList.h|| #include "CircularDoublyLinkedList.h" main.cpp int main() ! cheeks CircularDoublyLinkedList d; d.InsertRear("10"); head [ 0 d.InsertRear("30"); d.PrintList(); ! cout << d.DeleteRear() << endl; ‘| .PrintList(); ‘| } Minjoong Rim, Dongguk University 41 Data Structure

--- 페이지 42 ---
Class of Circular Doubly Linked List. #include <cstdlib> #include <string> using namespace std; typedef string Data; struct Node { // node for doubly linked list Data data; Node* next; Node®* prev; // next, previous Node(Data d="", Node* n=NULL, Node* p=NULL) { data = d; next = n; prev = p; } }; // all members are public #include <iostream> CircularDoublyLinkedList.h|| class CircularDoublyLinkedList { protected: Node head; void DeleteNode(Node”® p); // header node public: CircularDoublyLinkedList() { head = Node("", &head, &head); } void InsertFront(Data d); void InsertRear(Data d); Data PeekFront(); Data PeekRear‘(); Data DeleteFront(); Data DeleteRear(); void PrintList(); bool |lsEmpty() { return head.next == &head; } Minjoong Rim, Dongguk University Data Structure

--- 페이지 43 ---
Implementation of Circular Doubly Linked List. CircularDoublyLinkedList.cpp|| void CircularDoublyLinkedList::DeleteNode #include "CircularDoublyLinkedList.h" (Node* p) { // Delete node void Error(string s) { cerr << s; exit(-1); } if (6 == &head) Error("cannot delete"); p->prev->next = p->next; void CircularDoublyLinked_List::InsertFront p->next->prev = p->prev; (Data 0) { // Insert a node at the front delete p; Node* p = &head; } p->next = p->next->prev = new Node(d, p->next, p); Data CircularDoublyLinkedList::DeleteFront() } { // Delete node Data d = PeekFront(); void CircularDoublyLinked_List::InsertRear DeleteNode(head.next); (Data 0) { // Insert a node at the rear return d; Node* p = &head; } p->prev = p->prev->next = new Node(d, p, p->prev); Data CircularDoublyLinkedList::DeleteRear() { // Delete node Data 0 = PeekRear‘(); DeleteNode(head.prev); return d; } Minjoong Rim, Dongguk University 43 Data Structure

--- 페이지 44 ---
Implementation (continued). // continued CircularDoublyLinkedList.cpp|| void CircularDoublyLinkedList::PrintList() { // Print linked list cout << "[head] => "; Node* p; for (p = head.next; p != &head; p = p->next) cout << p->data <<" =>"; // forward Data CircularDoublyLinked_List::PeekFront() { // Get data of front node if (IsEmpty()) Error("List is empty"); return head.next->data; cout << "[tail]" << endl << "[tail] =>"; for (p = head.prev; p != &head; p = p->prev) cout << p->data <<" =>"; // backward cout << "[head]" << endl; } Data CircularDoublyLinked_List::PeekRear() { // Get data of rear node if (IsEmpty()) Error("List is empty"); return head.prev->data; } Minjoong Rim, Dongguk University 44 Data Structure

--- 페이지 45 ---
Music playlist. #include "CircularDoublyLinkedList.h" main.cpp class MusicList : public CircularDoublyLinkedList { Node* current; // current node to play bool IsHead() { return current == &head; } void Play() { if (IsHead()) return; // play music cout << current->data <<", ";} public: MusicList() { current = &head; } void DeleteCurrent() { // delete current node current = current->next; // go to next DeleteNode(current->prev); } // delete prev void PlayNext() { current = current->next; if (IsHead()) current = current->next; Play(); } void PlayPrev() { current = current->prev; if (IsHead()) current = current->prev; Play(); } }; // void InsertRear(Data d) inherited Minjoong Rim, Dongguk University 45 struct Node { Data data; Node* next; Node* prev; int main() MusicList m; |” string playlist[6] = {"Savage", "Antifragile", "Eleven", "Loco", "Tomboy", "Attention"}; for (int i = 0; i < 6; i++) { // insert music m.InsertRear(playlist[i]); } for (int i = 0; i < 9; i++) { // play forward m.PlayNext(); } cout << endl: m.DeleteCurrent(); // delete current music for (int i = 0; i < 9; i++) { // play backward m.PlayPrev(); } return 0; | class CircularDoublyLinkedList { } Node head; void DeleteNode(Node* 0); void InsertRear(Data 0); }; Data Structure

--- 페이지 46 ---
Supplementary Material Minjoong Rim, Dongguk University 46 Data Structure

--- 페이지 47 ---
ㆍ Queue ADT =» Insert (Enqueue) add an element to the rear of the queue =» Delete (Dequeue) remove an element from the front » Peek get an element of the front of the queue without removing » Is Queue Empty? Data Structure [ㅣ ㅣ ㅣ | Insert10 [io] | | | | Insert 20 [iofzo] | | | Insert 30 [so] 20130] | | Delete +10 [20]30] | | | Delete +20 [30] | | | | class Queue { void Insert(Data d); Data Delete(); Data Peek(); bool IsEmpty(); Minjoong Rim, Dongguk University 47 Data Structure

--- 페이지 48 ---
A queue is a data structure which is used to store data in a First In First Out (FIFO) order. #include <iostream> #include <cstdlib> using namespace 510; const int QUEVE_SIZE = 5; typedef int Data; void Error(string s) { cerr << s; exit(-1); } class Queue { // array representation of queue Data data[QUEUE_SIZE]; // queue array int front, rear, length; // first, last, # of data public: Queue() { front = 0; rear = -1; length = 0; } void Insert(Data 0) { // insert a new element if (IsFull()) Error("Queue is full"); rear = (rear + 1) % QUEUE_SIZE; data[rear] = d; length++; } Data Delete() { // delete oldest element if (IsEmpty()) Error("Queue is empty"); Data out = datal[front]; front = (front + 1) % QUEUE_SIZE; length--; return out; } Minjoong Rim, Dongguk University 48 main.cpp Data Peek() { // get oldest element if (IsEmpty()) Error("“Queue is empty"); return datal[front]; } bool IsEmpty() { return length == 0; } bool IsFull() { return length==QUEUE_SIZE; } }; reary front int main() { front, rear q.Insert(10); q.Insert(20); a a q.Insert(30); fron ear cout << qDelete(; [0100130| —T] cout << "": cout << q.Delete(); return 0; fron ear 개 rear Data Structure

--- 페이지 49 ---
Using circular list, queue can be implemented without traversing. main.cpp #include <iostream> #include <cstdlib> using namespace 510; typedef int Data; #define DELETE(a) { Node* temp = a; \ a = a->next; delete temp; } void Error(string s) { cerr << s; exit(-1); } class Node { // node for linked list Data data; Node* next; Node(Data d=0, Node* n=NULL) { data = 0; next = n; } // constructor friend class Queue; }; class Queue { // circular linked list Node® tail; // head = tail->next public: Queue() { tail = NULL; } void Insert(Data 0) { // insert at the tail if (IsEmpty()) {// if list is empty tail = new Node(d); tail->next = tail; } else { tail->next = new Node(d, tail->next); Minjoong Rim, Dongguk University 49 tail = tail->next; }} // move to fail Data Delete() { // delete head (tail->next) if (IsEmpty()) Error("Queue empty"); Data d = tail->next->data; // head data if (tail->next == tail) { // if only one node delete tail; tail = NULL; } // empty list else DELETE (tail->next); // delete head return d; } Data Peek() { // get oldest element if (IsEmpty()) Error("Queue is empty"); return tail->next->data; } nul] = tail 1! . tail int main() Queue 0; fal q.Insert(10); q.Insert(20); tail q.Insert(30); cout << q.Delete(); 20 = cout << "" << q.Delete(); —— tail = Data Structure

--- 페이지 50 ---
ㆍ Deque ADT Insert Front Prepend Insert Rear Append Delete Front Delete Rear Peek Front Peek Rear 16 Deque Empty? Minjoong Rim, Dongguk University | | ㅣ | InsertRear 10 110] [ [| ㅣ | InsertRear 20 110]20] ㅣ ㅣ | DeleteFront > 10[20] | | | | InsertFront 30 [sofzo[] ㅣ | | DeleteRear — 20 139] ㅣ ㅣ ㅣ | class Deque { void InsertFront(Data d); void InsertRear(Data d); Data DeleteFront(); Data DeleteRear(); Data PeekFront(); Data Data PeekRear‘(); Structure bool IsEmpty(); 50 Data Structure

--- 페이지 51 ---
In 06046, insertion and deletion operations are performed at both ends (front and rear). #include <iostream> main.cpp #include <cstdlib> using namespace sid; const int DEQUE_SIZE = 5; typedef int Data; void Error(string s) { cerr << s; exit(-1); } class Deque { // Deque is pronounced deck Data data[DEQUE_SIZE]; // deque array int front, rear, length; // first, last, # of data public: Deque() { front = length = 0; rear = DEQUE_SIZE - 1; } void InsertFront(Data d) { // insert to front if (IsFull()) Error("Deque is full"); front = (front - 1 + DEQUE_SIZE) % DEQUE_SIZE; data[front] = d; length++; } Minjoong Rim, Dongguk University 51 void InsertRear(Data 0) { // insert to rear if (ISFull()) Error("Deque is full"); rear = (rear + 1) % DEQUE_SIZE; data[rear] = d; length++; } Data DeleteFront() { // delete from front if (IsEmpty()) Error("Deque is empty"); Data out = data[front]; front = (front + 1) % DEQUE_SIZE; length--; return out; } Data DeleteRear() { // delete from rear if (IsEmpty()) Error("Deque is empty"); Data out = data[rear]; rear = (rear - 1 + DEQUE_SIZE) % DEQUE_SIZE; length--; return out; } Data PeekFront() { // get from front if (IsEmpty()) Error("Deque is empty"); return data[front]; } Data PeekRear‘() { // get from rear if (IsEmpty()) Error("Deque is empty"); return data[rear]; } Data Structure

--- 페이지 52 ---
(continued) bool IlsEmpty() { return length == 0; } bool IsFull() { return length==DEQUE_SIZE; } }; int main() { Deque d; d.InsertRear(10); d.InsertRear(20); cout << d.DeleteFront(); cout << ""; d.InsertFront(30); cout << d.DeleteRear(); return 0; Minjoong Rim, Dongguk University 52 Data Structure

--- 페이지 53 ---
In doubly linked list, a node contains next and previous pointers. #include <iostream> #include <cstdlib> using namespace 510; typedef int Data; void Error(string s) { cerr << s; exit(-1); } main.cpp class Node { // node for doubly linked list Data data; Node* next; Node®* prev; // next, previous Node(Data d=0, Node* n=NULL, Node* p=NULL) { data = d; next = n; prev =p; } friend class Deque; }; class Deque { // doubly linked list Node* head; Node tail; // pointer to first, last public: Deque() { head = tail = NULL; } Minjoong Rim, Dongguk University 53 void InsertFront(Data d) { // insert to front if (IsEmpty()) // if empty, single node head = tail = new Node(d); else // if not empty, insert between head = head->prev = new Node(d, head, NULL); } void InsertRear(Data d) { // insert to rear if (IsEmpty()) // if empty, single node head = tail = new Node(d); else // if not empty, insert between tail = tail->next = new Node(d, NULL, tail); } Data DeleteFront() { // delete from front if (IsEmpty()) Error("Queue is empty"); Data 0 = head->data; if (head->next == NULL) { delete head; head = tail = NULL; } else { head = head->next; delete head->prev; head->prev = NULL; } return d; } Data Structure

--- 페이지 54 ---
(continued) Data DeleteRear() { // delete from rear if (IsEmpty()) Error("“Queue is empty"); Data d = tail->data; if (tail->prev == NULL) { delete tail; head = tail = NULL; } else { tail = tail->prev; delete tail->next; tail->next = NULL; } return d; } Data PeekFront() { // get from front if (IsEmpty()) Error("“Queue is empty"); return head->data; } Data PeekRear‘() { // get from rear if (IsEmpty()) Error(“Queue is empty"); return tail->data; } }; Minjoong Rim, Dongguk University bool lsEmpty() { return head == NULL; } int main() head—» null null = tail Deque d; head d.InsertRear(10); d.InsertRear(20); cout << d.DeleteFront(); cout << "": d.InsertFront(30); cout << d.DeleteRear(); return 0; Data Structure

--- 페이지 55 ---
Including Node instance. #include <iostream> int main() using namespace std; { header node or typedef int Data: LinkedLista; ““™mmy node struct Node { // Node for linked list a.InsertFront(10); Data data; a.InsertFront(20); Node* next; // next node a.InsertFront(30); Node(Data d=0, Node* n=NULL) { a.PrintList(); data = 0; next = n; } return 0; | ull }; class LinkedList { Node head; // head node public: void InsertFront(Data d) { // insert to front head.next = new Node(d, head.next); } void PrintList() { cout << "[head] => "; for (Node* p = head.next; ; p != NULL; p = p->next) { cout << p->data << "=>"; } cout << "[null]" << endl; } }; Minjoong Rim, Dongguk University 55 Data Structure

--- 페이지 56 ---
Inherited from class Node. #include <iostream> using namespace std; typedef int Data; struct Node { // Node for linked list Data data; Node* next; // next node Node(Data d=0, Node* n=NULL) { data = 0; next = n; } }; class LinkedList : public Node { public: // inherited from class Node void InsertFront(Data d) { // insert to front next = new Node(d, next); } void PrintList() { cout << "[head] => "; for (Node* p = next; p != NULL; p = p->next) { cout << p->data << " => cout << "[null]" << endl; } }; Minjoong Rim, Dongguk University int main() { header node or LinkedList a; 9 “ummy node a.InsertFront(10); a.InsertFront(20); a.InsertFront(30); a.PrintList(); return 0; | ull "3 56 Data Structure

--- 페이지 57 ---
Without using class Node. #include <iostream> int main() using namespace std; { header node or typedef int Data: LinkedLista; ““™mmy node a.InsertFront(10); class LinkedList { // without using class Node a.InsertFront(20); Data data; a.InsertFront(30); LinkedList* next; // next node a.PrintList(); public: return 0; | ull LinkedList(Data d=0, LinkedList* n=NULL) { data=d; next=n; } void InsertFront(Data 0) { // insert to front next = new LinkedList(d, next); } void PrintList() { cout << "[head] =>"; for (LinkedList* p = next; p != NULL; p = p->next) { cout << p->data << "=>"; } cout << "[null]" << endl; } }; Minjoong Rim, Dongguk University 57 Data Structure

— 페이지 58 —
Minjoong Rim, Dongguk University 58 Data Structure
