# Django Framework 핵심
Django>=5.0,<6.0
asgiref==3.12.1
sqlparse==0.5.5
tzdata==2026.3

# 환경변수 (.env) 관리 (SECRET_KEY, DB 설정 등)
python-decouple==3.8

# PDF 텍스트 추출 (pypdfium2 + pytesseract OCR 폴백 파이프라인)
pypdfium2==5.12.1
pytesseract==0.3.13
pillow==12.3.0

# 테스트 픽스처 생성용 (암호화 PDF 등 테스트 데이터 제작에만 사용, 프로덕션 코드에서는 미사용)
pypdf==6.14.2

# PDF 파싱 처리용 암호화 및 C 바인딩 암시적 의존성
cryptography==49.0.0
cffi==2.1.0
pycparser==3.0
charset-normalizer==3.4.9

# AI 분석 연동
google-genai==2.16.0

# 소셜 로그인
django-allauth[socialaccount]==65.18.0

# 배포용
gunicorn==23.0.0
psycopg[binary]==3.2.13
dj-database-url==2.3.0
whitenoise==6.8.2

# 업로드 파일(PDF) 영구 저장용
django-storages[google]==1.14.4
google-cloud-storage==2.19.0